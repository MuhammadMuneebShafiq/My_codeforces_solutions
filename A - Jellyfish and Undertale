for _ in range(int(input())):
    a , b , n = map(int,input().split())
    arr = list(map(int,input().split()))
    sm = b
    for r in arr:
        if r >= a:
            sm += (a-1)
        else:
            sm += r
    print(sm)
